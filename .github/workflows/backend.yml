name: Deploy Backend

on:
  push:
    branches: [ "main" ]
    paths:
      - 'backend/**'
      - 'frontend/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ✅ Java setup
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # ✅ Node FIXED (important)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ✅ Build frontend
      - name: Install and build frontend (production)
        working-directory: ./frontend
        env:
          VITE_API_URL: 'https://api.broandbro.in'
        run: |
          npm ci
          npm run build

      # ✅ Copy frontend into backend
      - name: Copy frontend dist into backend static resources
        run: |
          mkdir -p backend/src/main/resources/static
          rm -rf backend/src/main/resources/static/*
          cp -a frontend/dist/. backend/src/main/resources/static/

      # ✅ Build JAR
      - name: Build JAR
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: '8.6'
          arguments: ':backend:bootJar -x test'
        env:
          SPRING_PROFILES_ACTIVE: prod
          SPRING_DATASOURCE_URL: ${{ secrets.SPRING_DATASOURCE_URL }}
          SPRING_DATASOURCE_USERNAME: ${{ secrets.SPRING_DATASOURCE_USERNAME }}
          SPRING_DATASOURCE_PASSWORD: ${{ secrets.SPRING_DATASOURCE_PASSWORD }}
          RAZORPAY_KEY: ${{ secrets.RAZORPAY_KEY }}
          RAZORPAY_SECRET: ${{ secrets.RAZORPAY_SECRET }}

      # ✅ Rename JAR
      - name: Rename JAR to app.jar
        run: |
          cd backend/build/libs
          JAR_FILE=$(ls *.jar | head -n 1)
          mv "$JAR_FILE" app.jar

      # 🧹 Clean VPS folder (safe reset)
      - name: Clean VPS app folder
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            mkdir -p /home/ubuntu/app
            rm -f /home/ubuntu/app/app.jar

      # ✅ Upload JAR (FIXED)
      - name: Upload to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          source: "backend/build/libs/app.jar"
          target: "/home/ubuntu/app/"
          overwrite: true

      # 🚀 Restart backend
      - name: Restart Backend
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/ubuntu/app
            echo "Files in app directory:"
            ls -lh

            if [ ! -f app.jar ]; then
              echo "ERROR: app.jar not found!"
              exit 1
            fi

            pm2 stop backend || true
            pm2 delete backend || true

            pm2 start "SPRING_DATASOURCE_URL='${{ secrets.SPRING_DATASOURCE_URL }}' \
            SPRING_DATASOURCE_USERNAME='${{ secrets.SPRING_DATASOURCE_USERNAME }}' \
            SPRING_DATASOURCE_PASSWORD='${{ secrets.SPRING_DATASOURCE_PASSWORD }}' \
            RAZORPAY_KEY='${{ secrets.RAZORPAY_KEY }}' \
            RAZORPAY_SECRET='${{ secrets.RAZORPAY_SECRET }}' \
            APP_CORS_ALLOWED_ORIGINS='${{ secrets.APP_CORS_ALLOWED_ORIGINS }}' \
            java -jar app.jar --spring.profiles.active=prod" --name backend

            pm2 save